const colors = {
  black: "#000",
  charcoal: "#4A4A4A",
  electricIndigo: "#8F00FF",
  lime: "#14FF00",
  neonCarrot: "#FF9B3B",
  slateBlue: "#6B5CFF",
  white: "#FFF"
};

const eventTypes = {
  REACT_NATIVE_INFO: "update_REACT_NATIVE_INFO",
  SET_ACTIVE_TYPE: "set_ACTIVE_TYPE"
};

const types = {
  DEFAULT: "default",
  SMS: "sms",
  PHOTO: "photos",
  CONTACT: "contacts",
  CALL: "appels"
};

const MAX_PARTICLES = 3200;

export { colors, eventTypes, types, MAX_PARTICLES };
